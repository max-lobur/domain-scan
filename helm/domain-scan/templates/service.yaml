apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.nginx.appName }}
  namespace: {{ .Values.nginx.namespace }}
spec:
  selector:
    app: {{ .Values.nginx.appName }}
  ports:
    - protocol: "TCP"
      name: http
      port: 80
      targetPort: 80
  type: ClusterIP
